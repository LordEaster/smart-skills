# Plan: <task name>

**Goal:** <1 sentence>
**Non-goals:** <bullets — omit section if none>
**Verify with:** <typecheck/build/test commands; if none exist, a concrete runtime check — e.g. "boot the app, curl the endpoint">

## Phases

1. <phase name> — <1–2 sentence scope> — files: <paths, or "agy explores">
2. <phase name> — <1–2 sentence scope> — files: <paths>
3. ...

Rules: ≤5 files per phase where possible, one concern per phase, ≤300 words total for the whole plan.
